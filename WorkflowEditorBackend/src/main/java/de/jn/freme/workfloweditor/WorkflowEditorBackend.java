package de.jn.freme.workfloweditor;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class WorkflowEditorBackend {

	public static void main(String[] args) {

//		new SpringApplication(WorkflowEditorBackend.class).run(args);
		SpringApplication.run(WorkflowEditorBackend.class, args);
	}

}
